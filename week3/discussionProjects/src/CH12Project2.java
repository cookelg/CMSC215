/**
 * Write the bin2Dec(String binaryString) method to convert a binary string into
 * a
 * decimal number
 * the method thows a NumberFormatException if the string is not a binary String
 * Write a main method that prompts the user to enter a binary number as a
 * string
 * and displays decimal equivelant of the string
 * if the method thrown an exception, display "Not a binary number"
 */
